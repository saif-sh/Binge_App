import 'package:flutter/material.dart';

final ButtonStyle buttonprim = ElevatedButton.styleFrom(
    minimumSize: Size(308, 42),
    primary: Color.fromARGB(255, 63, 85, 198),
    elevation: 0,
    shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(50))));
